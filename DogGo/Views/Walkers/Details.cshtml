@model DogGo.Models.ViewModels.WalkerProfileViewModel

@{
    ViewData["Title"] = "Profile";
}

<h1 class="mb-4">@($"Walker's Profile")</h1>


<div>
       
        <hr />
        <div class="card">
           
            <h3 class="mb-4">@($"{Model.Walker.Name}")</h3>
            <img src="@Model.Walker.ImageUrl" width="60" height="60" alt="avatar" />
            <p class="mb-4">@($"{Model.Walker.Neighborhood.Name}")</p>
          
        </div>
    </div>
    <div>
@Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
<a asp-action="Index">Back to List</a>
</div>
